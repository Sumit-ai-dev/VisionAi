{
  "name": "visionai-nexus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/server/index.ts",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "lint": "node scripts/lint.mjs",
    "typecheck": "tsc --noEmit",
    "checkup": "npm run typecheck && npm run lint && npm test && npm run build && node scripts/smokeCheck.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "openai": "^4.56.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.5.4",
    "tsx": "^4.19.0",
    "vite": "^5.4.2",
    "vitest": "^1.6.0"
  }
}
